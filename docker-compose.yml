version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DEBUG=False
      - SECRET_KEY=django-insecure-^f9@*v7*h@exrj&#6wyrz)yc2(wpu81u0zjuf&*6*2%c12$@g
      - DB_NAME=shizaizhineng
      - DB_USER=lxyuser
      - DB_PASSWORD=Lxy1463240856!
      - DB_HOST=db
      - DB_PORT=3306
      - REDIS_URL=redis://redis:6379/0
      - SITE_URL=http://localhost:8000
    depends_on:
      - db
      - redis

  db:
    image: registry.cn-hangzhou.aliyuncs.com/library/mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=Lxy1463240856!
      - MYSQL_DATABASE=shizaizhineng
      - MYSQL_USER=lxyuser
      - MYSQL_PASSWORD=Lxy1463240856!
    volumes:
      - mysql-data:/var/lib/mysql

  redis:
    image: registry.cn-hangzhou.aliyuncs.com/library/redis:6.2
    volumes:
      - redis-data:/data

volumes:
  mysql-data:
  redis-data:
